# Demo Data

All data in `demo-data/` is **synthetic** and generated for **SOC alert investigation**.

- No real credentials, PII, or production telemetry
- Safe to commit and share publicly
- Regenerate with `python scripts/seed_demo_data.py`
